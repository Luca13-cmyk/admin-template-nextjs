import Layout from "../components/template/Layout";

// import useAppData from "../data/hook/useAppData";

export default function Notificacoes() {
  // const { alternarTema } = useAppData(); // Dados

  return (
    <Layout
      titulo="Notificacoes"
      subtitulo="Aqui ira gerenciar as notificacoes"
    >
      <h1>conteudo</h1>
    </Layout>
  );
}
